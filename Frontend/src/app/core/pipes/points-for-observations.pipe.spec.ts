import { PointsForObservationsPipe } from './points-for-observations.pipe';

describe('PointsForObservationsPipe', () => {
  it('create an instance', () => {
    const pipe = new PointsForObservationsPipe();
    expect(pipe).toBeTruthy();
  });
});
