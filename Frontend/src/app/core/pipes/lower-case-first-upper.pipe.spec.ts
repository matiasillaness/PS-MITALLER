import { LowerCaseFirstUpperPipe } from './lower-case-first-upper.pipe';

describe('LowerCaseFirstUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new LowerCaseFirstUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
